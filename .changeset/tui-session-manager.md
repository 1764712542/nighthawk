---
"@nighthawk/nighthawk": minor
---

Add the `/session` command to browse every session across workspaces: `Ctrl+E` expands a session's details and `Delete` deletes one after a `[y/N]` confirmation.
