{
  "name": "@nighthawk/protocol",
  "version": "0.5.0",
  "private": true,
  "description": "Shared REST + WS protocol schemas (envelope, error codes, pagination, ws-control) for the nighthawk daemon.",
  "license": "MIT",
  "author": "NightHawk Contributors",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/1764712542/nighthawk.git",
    "directory": "packages/protocol"
  },
  "bugs": {
    "url": "https://github.com/1764712542/nighthawk/issues"
  },
  "type": "module",
  "imports": {
    "#/*": "./src/*.ts"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ulid": "^3.0.1",
    "zod": "^4.3.6"
  }
}
