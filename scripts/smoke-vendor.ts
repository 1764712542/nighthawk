import * as tui from '@nighthawk/pi-tui';
console.log('pi-tui exports:', Object.keys(tui).length, '->', Object.keys(tui).slice(0, 25).join(', '));
import * as ks from '@nighthawk/kosong';
console.log('kosong exports:', Object.keys(ks).length, '->', Object.keys(ks).slice(0, 20).join(', '));
