# transcript 包概览

本页介绍 packages/transcript 包的定位、版本、目录结构和依赖。

## 定位

Isomorphic transcript rendering data layer: agent-granular L1 store, idempotent L2 operations, granularity-gated L3 subscriptions, framework-free L4 view registry.

## 元数据

| 属性 | 值 |
| --- | --- |
| 包名 | `@nighthawk/transcript` |
| 版本 | 0.0.2 |
| 说明 | Isomorphic transcript rendering data layer: agent-granular L1 store, idempotent L2 operations, granularity-gated L3 subscriptions, framework-free L4 view registry. |
| 源码文件数 | 24 |
| 测试文件数 | 2 |
| 顶层源码目录 | contract, granularity, history, index.ts, model, ops, pagination, store, view |
| 主要 scripts | build, typecheck, test, clean |
| 依赖数 | 1 |

## 顶层模块

主要顶层源码文件/目录：`contract`, `granularity`, `history`, `index.ts`, `model`, `ops`, `pagination`, `store`, `view`。

## 测试规模

测试文件约 2 个；测试入口通常在 `packages/transcript/test/`。

## 核心实现细节（源码导出）

以下是本文涉及路径中的真实源码导出/结构，帮助你把概念映射到函数与类：

  - `packages/transcript/package.json`（非 TS 源码，可直接阅读）
  - `packages/transcript/package.json`（非 TS 源码，可直接阅读）

## 证据与代码位置

- `packages/transcript/package.json`
- `packages/transcript/package.json`

> 本文所有路径均相对仓库根目录；引用内容以仓库当前 `HEAD` 为准。
