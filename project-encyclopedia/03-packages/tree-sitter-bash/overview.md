# tree-sitter-bash 包概览

本页介绍 packages/tree-sitter-bash 包的定位、版本、目录结构和依赖。

## 定位

A pure-TypeScript bash parser producing a syntax tree whose named node types match tree-sitter-bash one-to-one.

## 元数据

| 属性 | 值 |
| --- | --- |
| 包名 | `@nighthawk/tree-sitter-bash` |
| 版本 | 0.1.0 |
| 说明 | A pure-TypeScript bash parser producing a syntax tree whose named node types match tree-sitter-bash one-to-one. |
| 源码文件数 | 7 |
| 测试文件数 | 8 |
| 顶层源码目录 | budget.ts, grammar.ts, index.ts, lexer.ts, node.ts, parse.ts, parser.ts |
| 主要 scripts | build, typecheck, test, clean |
| 依赖数 | 2 |

## 顶层模块

主要顶层源码文件/目录：`budget.ts`, `grammar.ts`, `index.ts`, `lexer.ts`, `node.ts`, `parse.ts`, `parser.ts`。

## 测试规模

测试文件约 8 个；测试入口通常在 `packages/tree-sitter-bash/test/`。

## 证据与代码位置

- `packages/tree-sitter-bash/package.json`
- `packages/tree-sitter-bash/README.md`

> 本文所有路径均相对仓库根目录；引用内容以仓库当前 `HEAD` 为准。
