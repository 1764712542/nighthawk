# security-core 包概览

本页介绍 packages/security-core 包的定位、版本、目录结构和依赖。

## 定位

NightHawk monorepo 内的一个 TypeScript 包。

## 元数据

| 属性 | 值 |
| --- | --- |
| 包名 | `@nighthawk/security-core` |
| 版本 | 0.1.0 |
| 说明 | — |
| 源码文件数 | 25 |
| 测试文件数 | 0 |
| 顶层源码目录 | cli, config, core, security, tools, ui |
| 主要 scripts |  |
| 依赖数 | 0 |

## 顶层模块

主要顶层源码文件/目录：`cli`, `config`, `core`, `security`, `tools`, `ui`。

## 测试规模

测试文件约 0 个；测试入口通常在 `packages/security-core/test/`。

## 核心实现细节（源码导出）

以下是本文涉及路径中的真实源码导出/结构，帮助你把概念映射到函数与类：

  - `packages/security-core/package.json`（非 TS 源码，可直接阅读）
  - `packages/security-core/package.json`（非 TS 源码，可直接阅读）

## 证据与代码位置

- `packages/security-core/package.json`
- `packages/security-core/package.json`

> 本文所有路径均相对仓库根目录；引用内容以仓库当前 `HEAD` 为准。
